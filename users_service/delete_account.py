# standard python imports

# our imports
from utils import generate_success_response

dataType = {
}

def delete_account(data: dataType, conn, logger):
    generate_success_response("Successfully changed password")
